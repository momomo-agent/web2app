const { init } = require('./init')
const { build } = require('./build')
const { run } = require('./run')
const { doctor } = require('./doctor')
const { icon } = require('./icon')

module.exports = { init, build, run, doctor, icon }
